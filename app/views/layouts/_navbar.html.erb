<header>
  <a href="/">Tasklist</a>
  
  <% if logged_in? %>
    <%= current_user.name %>
    <%= link_to 'Logout', logout_path, method: :delete %>
  <% else %>
    <%= link_to 'Signup', signup_path %>
    <%= link_to 'Login', login_path %>
  <% end %>
  
</header>